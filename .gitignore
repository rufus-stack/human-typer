# Build artifacts
build/
dist/
__pycache__/
*.pyc

# macOS cruft
.DS_Store

# Icon build intermediates (keep icon.icns / icon.ico)
AppIcon.iconset/
icon_1024.png

# NEVER commit or ship the plaintext keys.
LICENSE_KEYS.txt

# Local secrets for seeding (Supabase service_role etc.) — never commit.
.env
.env.*
